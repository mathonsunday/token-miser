#!/bin/sh

# Commitlint — only enforced in mature phase

PHASE=$(cat .claude/phase 2>/dev/null | tr -d '\n' || echo "vibe")

if [ "$PHASE" = "mature" ]; then
  npx --no -- commitlint --edit "$1"
fi
